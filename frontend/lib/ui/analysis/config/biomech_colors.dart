import 'package:flutter/material.dart';

abstract class BiomechColors {
  static const background = Color(0xFF101012);
  static const card = Color(0xFF141416);
  static const screenBackground = Color(0xFF161618);
  static const optimal = Color(0xFF00D4AA);
  static const warning = Color(0xFFFFB627);
  static const error = Color(0xFFFF3B30); // Ejemplo adicional
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white54;
}
